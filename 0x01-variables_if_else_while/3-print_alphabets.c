#include <stdio.h>
/**
 * main - A function that prints alphabet in lowercase and uppercase
 * Return: 0
 */

int main(void)
{
	char k;

	char n;

	k = 'a';
	n = 'A';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
