#include <stdio.h>
/**
 * main - A function that prints alphabet in lowercase with a new line
 * Return: 0
 */

int main(void)
{
	char k;

	k = 'a';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	putchar('\n');
	return (0);
}
