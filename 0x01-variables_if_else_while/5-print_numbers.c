#include <stdio.h>
/**
 * main - Function that prints digit numbers of base 10 with a new line
 * Return: 0
 */

int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
		printf("%d", k);
	printf("\n");
	return (0);
}
