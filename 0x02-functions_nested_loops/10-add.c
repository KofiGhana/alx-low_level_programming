#include "main.h"
/**
 * add - Function that adds two integers and returns the result
 * @a: The computed first number
 * @b: The computed second number
 * Return: Integer
 */

int add(int a, int b)
{
	return (a + b);
}
